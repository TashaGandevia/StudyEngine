// Zone 5 challenge bank. Populated by CONTENT-Z5 (M3) following the shared
// schema in ./schema.js. Empty for now so the run builder and type router have
// a real (if empty) source to operate on.
export default [];
